# Supabase — copie para .env e preencha com suas credenciais
# Sem estas variáveis, o sistema usa armazenamento local (localStorage)
# Project Settings → API → Project URL e anon public key

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
